# Pull Request: feat(blueprint): 新增Credit Agreement精准分类体系与分阶段性能优化落地路径

**目标文件**：美国贷款合同爬虫蓝图.md  
**变更类型**：功能设计增补 + 技术方案落地细化

---

## Summary
针对当前爬虫存在的「8-K附件泛匹配、无信贷协议精准分类」与「批量处理下内存溢出、资源竞争」两大核心问题，本PR对现有蓝图文档进行体系化补充：
1. 设计**分层加权特征分类器**，实现Credit Agreement前置精准判定，过滤无效债券类、承诺函类附件，从源头降低无效计算
2. 细化**三阶段性能优化落地路径**，从最小侵入的流式读取改造，到生产者-消费者架构解耦，再到可观测性探针建设，逐步解决索引OOM、大文件内存穿透、I/O与CPU资源叠加问题
3. 所有方案严格遵循SEC EDGAR Fair Access访问规范，兼容现有`py-sec-edgar`下载器基础能力，不破坏下游提取与CSV输出逻辑

## Problem Statement
### 模式识别侧
当前仅通过`loan`/`credit`关键词粗筛8-K附件，无信贷协议分类能力，导致大量非目标文件（债券契约Indenture、票据发行协议、信贷承诺函、担保协议等）进入提取流程，既浪费计算与存储资源，也向结果数据引入噪声。

### 性能优化侧
1. **全量索引OOM风险**：长历史CIK一次性加载全部提交记录，数据量随年限线性增长，极易触发内存溢出
2. **大文件内存穿透**：超大附件（含长表格、扫描件转录文本）全量读取到内存，单文件即可击穿内存阈值
3. **资源竞争崩溃**：下载（I/O密集型）与提取（CPU密集型）耦合执行，峰值内存与算力叠加，是进程崩溃的核心诱因

## Proposed Changes
### 一、模式识别：Credit Agreement分层分类体系
在「附件下载完成后 → 信息提取前」新增前置分类节点，采用**「标题快速过滤 + 内容加权评分」两级判定逻辑**，兼顾性能与准确率。

#### 1. 第一级：标题快速过滤（毫秒级，零额外内存开销）
基于附件文件名、EDGAR提交元数据中的文档标题字段做预判定，不匹配直接跳过，不读取文件内容。
- **强通过规则**（不区分大小写、忽略特殊字符与空格）：
  标题完整包含 `credit agreement` 或 `loan agreement`，且未命中排除词 → 直接判定为目标文件，进入提取流程
- **强排除规则**：
  标题包含 `indenture` / `note` / `bond` / `debenture` / `security instrument` 任意债券类核心关键词 → 直接跳过
- 标题模糊（仅含`credit`/`loan`但无`agreement`，或标题字段缺失）→ 进入第二级内容校验

#### 2. 第二级：内容加权评分判定
构建特征评分体系，**流式逐块扫描文本**，累计得分达到阈值或命中强排除项时提前终止读取，不加载全文。

| 特征维度 | 权重分值 | 判定规则 |
|---------|---------|---------|
| 合同主体特征 | 25分 | 同时出现 `Borrower` 与 `Lender` 的定义式表述（匹配 `"Borrower" means ...` 类典型句式） |
| 章节结构特征 | 25分 | 命中 `Article I`/`Section 1.01` 类标准章节编号，且包含 `Definitions`、`The Loans` 至少2个典型章节名 |
| 核心条款特征 | 30分 | 命中 `Interest Rate`、`Maturity Date`、`Events of Default`、`Covenants`、`Representations and Warranties` 中至少3项 |
| 排除惩罚项 | -50分/项 | 正文出现 `Indenture`、`Notes`、`Bond`、`Debenture` 任一债券类核心术语 |

- **判定阈值**：总分 ≥ 60分 → 认定为有效Credit Agreement，进入提取流程；否则跳过
- **流式优化**：按4KB分块读取文本，逐块累计得分，得分达标/触发强排除时立即关闭文件句柄，最大程度降低内存占用

---

### 二、性能优化：三阶段落地路径
#### Phase 1：最小侵入内存优化（P0优先级，改动量最小，快速验证收益）
**核心目标**：解决大文件内存穿透与索引全量加载问题，内存峰值降低60%以上，不改动整体业务架构。

1. **附件文本读取改造**
   - 替换全量 `read()` 为生成器模式分块读取（默认`chunk_size=4KB`），关键词匹配逐块执行，块处理完成立即释放内存
   - 新增 `mmap` 适配逻辑：单文件体积 ≥ 50MB 时，使用`mmap`模块实现零拷贝内存映射，正则匹配直接在映射区执行，避免用户态内存全量加载
   - 分类匹配逻辑同步适配流式读取，支持提前终止文件读取，不读取无效内容

2. **索引拉取流式改造**
   - 替换单CIK全量提交记录拉取，改为**按年度滚动拉取**：基于EDGAR `/edgar/full-index` 按年份分片，逐年拉取索引、逐年处理，处理完成后释放当年索引数据
   - 支持增量模式：可配置起始年份/日期，仅拉取目标范围内的提交记录，避免加载冗余历史数据

#### Phase 2：生产者-消费者架构解耦（P1优先级，核心架构升级）
**核心目标**：彻底隔离下载I/O与提取CPU负载，消除资源叠加峰值，支持水平扩展与断点续爬。

1. **角色拆分**
   - **Producer（下载生产者）**：单进程运行，内置令牌桶限速器，严格控制全局请求速率 ≤ 10 req/s，完全符合SEC Fair Access政策；负责按索引下载附件并落盘本地，完成后将文件路径与元数据写入任务队列
   - **Consumer（提取消费者）**：多进程池（默认进程数 = CPU核心数 - 1），从任务队列消费任务，依次执行分类判定、字段提取、CSV流式写入；单文件处理完成立即释放内存
2. **队列设计**
   - 采用 `multiprocessing.Queue` 作为内存任务队列，设置队列长度上限（默认1000），生产者在队列满时阻塞，避免队列无限增长打爆内存
   - 可选扩展：任务元数据落盘SQLite，支持断点续爬，异常重启后无需重复下载已完成文件
3. **收益**：下载I/O峰值与提取CPU峰值错峰，内存占用由「下载+提取叠加」变为「单环节峰值」，进程稳定性大幅提升

#### Phase 3：可观测性与自适应调控（P2优先级，生产级保障）
**核心目标**：建立运行状态监控能力，支持异常预警与自适应调优。

1. **监控探针**
   - 内存监控：每处理10个文件输出当前进程RSS内存占用，超过系统内存70%阈值时触发强制GC
   - 速率监控：统计实时请求QPS、下载成功率、分类通过率、字段提取成功率，每小时输出统计报表
   - 异常告警：请求429限流、大文件处理失败、内存超阈值等场景输出ERROR级别日志
2. **自适应调控**：当系统内存占用超过设定阈值时，自动暂停生产者生产，待消费者消费至水位线以下后自动恢复生产

## Implementation Roadmap
| 阶段 | 改动范围 | 预计工作量 | 优先级 |
|-----|---------|-----------|-------|
| Phase 1 | 分类器实现 + 文件读取改造 + 索引流式拉取 | 2-3人天 | P0 |
| Phase 2 | 生产者-消费者架构重构 + 断点续爬能力 | 3-4人天 | P1 |
| Phase 3 | 监控探针 + 自适应调控逻辑 | 1-2人天 | P2 |

## Acceptance Criteria
1. **分类效果**：测试样本集上，Credit Agreement召回率 ≥ 95%，精确率 ≥ 90%，债券类文件误判率 ≤ 2%
2. **内存表现**：Phase 1落地后，处理100MB级附件时进程内存峰值 ≤ 200MB；长历史CIK索引加载内存降低70%以上
3. **合规性**：全局请求速率稳定 ≤ 10 req/s，无SEC IP封禁风险
4. **稳定性**：Phase 2落地后，连续批量处理10000份附件无进程崩溃、无OOM异常
5. **兼容性**：保留原有CSV输出格式与字段提取逻辑，不破坏现有下游数据链路

## Additional Notes
1. 分类器阈值与特征权重支持配置化，便于后续基于真实样本迭代优化
2. 所有限速逻辑统一收敛到生产者层，避免多消费者重复限速导致全局速率超标
3. 需补充单元测试：针对分类器构建正负样本测试集，覆盖标题缺失、混合关键词等边界场景
4. 扫描件类PDF附件（无文本层）建议在分类前置环节直接跳过，避免无效OCR开销
